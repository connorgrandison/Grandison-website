import fs from 'node:fs';
import path from 'node:path';
const routes=JSON.parse(fs.readFileSync('routes.json','utf8'));const errors=[];const titles=new Set();const descriptions=new Set();
for(const route of routes){const filename=route.url==='/404/'?'dist/404.html':path.join('dist',route.url,'index.html');const html=fs.readFileSync(filename,'utf8');const title=html.match(/<title>(.*?)<\/title>/)?.[1];const description=html.match(/name="description" content="([^"]*)"/)?.[1];if(!title||titles.has(title))errors.push(`${route.url}: missing or duplicate title`);titles.add(title);if(!description||descriptions.has(description))errors.push(`${route.url}: missing or duplicate description`);descriptions.add(description);if((html.match(/<h1\b/g)||[]).length!==1)errors.push(`${route.url}: H1 count`);for(const m of html.matchAll(/(?:href|src)="(\/[^"?#]*)(?:[?#][^"]*)?"/g)){const ref=m[1];const target=path.join('dist',ref);if(!fs.existsSync(target)&&!fs.existsSync(path.join(target,'index.html')))errors.push(`${route.url}: missing ${ref}`)}for(const m of html.matchAll(/href="#([^"]+)"/g))if(!html.includes(`id="${m[1]}"`))errors.push(`${route.url}: missing #${m[1]}`);for(const m of html.matchAll(/<script type="application\/ld\+json">(.*?)<\/script>/gs)){try{JSON.parse(m[1])}catch{errors.push(`${route.url}: invalid schema`)}}}
if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`Verified ${routes.length} pages: unique metadata, one H1, local links, anchors and valid structured data.`);
